# Demiurge Agent Harness

本页只作稳定入口；按任务从架构和记忆中渐进拉取上下文。

## 上下文与行为

- 全部文本、命令输出和文件使用 UTF-8。
- 只做用户明确要求；不清楚时只问一个关键问题。
- 不说废话，不虚报完成，不用未经验证的假设代替证据。
- 先读 `CONTEXT.md`、`ARCHITECTURE.md` 与 `docs/memory/MEMORY.md` 的相关部分。
- 先确认目录、运行时、现有实现和测试基线，禁止凭印象开工。
- 跨层任务拆成可独立验证的纵向小步，避免一步到位留下半成品。
- 只加载当前任务需要的文件和工具，控制上下文与 Token 熵增。

## Harness 反馈闭环

- 遵循：复现或定义验收 → 定位接缝 → 实施 → 验证 → 修复 → 再验证。
- 可见界面、编译成功或写完代码都不等于完成；必须验证真实路径。
- 完成前重读用户要求、检查 diff、逐项核对验收条件和未决问题。
- 难查故障按复现→最小化→假设→插桩→修复→回归测试推进。
- Agent、RAG、异步队列故障优先查 Trace 和真实输入输出，不猜流程。
- 同一方案连续失败两次，停止小修，重新检查假设并换根本方案。
- 每次重复错误应固化为测试、linter、架构合同或项目记忆。
- 改代码后同步修正文档漂移；禁止让进度表和实现长期不一致。

## 修改与安全边界

- 工作区可能已有用户改动；不得覆盖、回滚或整理无关内容。
- 搜索优先 `rg`；代码修改使用 `apply_patch`。
- 禁止 `git reset --hard`、`git checkout --` 等破坏性操作。
- 删除、迁移前核对精确目标；禁止对宽泛目录递归操作。
- 密钥只进被 Git 忽略的运行态文件，禁止写入源码、日志和文档。
- 未经明确要求，不提交、不推送、不改外部系统状态。

## 架构合同

- 后端保持路由薄、服务深；`services` 禁止依赖 `routers`。
- 前端组件只负责展示；状态、编排和纯逻辑放进 `lib`。
- 配置、协议、清洗规则和数据真源必须只有一个属主。
- 新事件同步更新后端协议、前端解码、reducer 与双端测试。
- 会话快照是历史真源；已删除消息不得被缓存或旧状态复活。
- 自动插画按 `messageId + slotId` 原位回填，不追加对话轮。
- 自动插画后台运行，不得占用或阻塞前台聊天队列。
- 约束优先落为测试、类型或 import-linter，不只写提示词。
- 跨层决策更新 `ARCHITECTURE.md`；记忆同步两份 memory。

## PreCompletion 门禁

- 新功能和缺陷修复必须补覆盖真实失败路径的回归测试。
- 后端改动运行 Ruff、import-linter、mypy、硬编码检查及 pytest。
- 前端改动运行定向测试、`npm test` 与 `npm run build`。
- 只改单侧可跳另一侧；跨层改动必须验证两侧及接口契约。
- 使用项目配置的解释器和依赖；命令缺失时先定位环境，禁止跳检。
- 运行 `git diff --check`，确认没有夹带、乱码、密钥或生成产物。
- 既有失败须说明具体用例和影响，不得将部分通过描述为全绿。
- 完成报告列出实际测试、结果、运行态验证和剩余问题。
