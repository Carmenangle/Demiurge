# Demiurge — AGENT.md

面向在本项目上工作的 AI/工程师（非终端用户），不随发布包分发。
本页是入口，不是手册。先读它对齐「要造什么、约束是什么」，细节按需拉取。

> Demiurge（δημιουργός）：塑造并维系整个世界的工匠造物主。本产品让创作者成为造物主。

---

## 一、这是什么（North Star）

Demiurge 是**以「结构化剧本/故事」为唯一事实源的多元演绎创作平台**，从当前 ComfyUI 包装项目独立复制而来。

它不是「复刻酒馆」，也不是「更好的酒馆」，而是新品类：
- **酒馆只占「文本演绎」一格**；Demiurge 还有图/视频/gif 的**多元演绎**。
- 事实源不是「一条聊天 log」（线性、长了就崩），而是**结构化剧本**（角色、场景、时间线）。文本、图、视频全部从它生成、并回锚到它。
- **一切挂在这根脊柱上 = 平台；各功能各玩各的 = 工具堆。** 这是成败分界线。

### 核心联动链（产品心脏）
```
文本演绎（剧情推进）
  → ① 识别剧情高潮点              [新造：LLM 监测叙事张力峰值]
  → ② 结构化提取：角色外貌/动作/构图 [新造：LLM 结构化抽取]
  → ③ 组装提示词 + 挂角色 LoRA/参考图 [新造 + 复用一致性资产]
  → ④ 填进工作流模板的提示词节点     [★已有★ 当前项目的节点注入]
  → ⑤ 出图/视频/gif                [★已有★ 现有管线]
```
下半截（④⑤）当前项目已具备，酒馆层负责**产出提示词**，现有管线负责**消费出图**，对接口就是「提示词节点」。**真正要新造的只有上半截 ①②③。**

---

## 二、与当前项目的关系（独立副本，非共享）

- Demiurge 是**独立副本 + 独立仓库**，可自由大改不影响现有产品。
- 代价：**双份维护**——老项目修的 bug、加的能力，本项目不会自动获得，需手动同步。接受此代价换取自由改造。
- 已搬（地基）：`backend/` 全部源码、门禁配置、统一模型设置代码、`comfyui-ext/laf_lock/`、前端骨架（`api/`+`lib/`+`stores/`+`types/`+构建配置）。
- **未搬（指向或重建，不复制）**：模型权重（设置指向同一目录）、RAG 索引（先指向老索引、数据成型后重建）、runtime 产物（独立重建）、`.git`、`node_modules/`、`dist/`。
- **红线未搬**：`backend/data/` 的密钥/用户态（`user_state.json` 等）。密钥只允许落在被 Git 忽略的 `backend/data/user_state.json`，不得进源码、日志、发布包。

---

## 三、模型设置（复用，不另造）

对话模型 / 生图模型 / 嵌入·重排模型统一走**同一份现有模型配置**。酒馆文本演绎接对话模型、剧情生图接生图模型、RAG 接嵌入模型，不为酒馆另做设置。

---

## 四、前端 UI 改革方向

从「工具面板」改成「围绕一份剧本的创作台」，像酒馆一样精简，去掉主题/装饰素材。三模式顶部切换，不堆一屏：
- **演绎态**（主界面）：中间剧情流；高潮点自动把图/视频/gif **内嵌在剧情里**；右侧收起式抽屉放当前场景角色/构图。
- **写作态**：剧本结构——角色、世界设定、时间线、分章。
- **资产态**：角色卡（绑 LoRA/参考图，管一致性）、世界书（走 RAG）、工作流模板库。

**重资产界面**（节点管理/模板画布/主题）降级进「高级/设置」，日常不碰、按需展开——渐进披露。
**正则 / 预设编辑**（破甲关键）作为通用创作者功能规划进新架构，在资产态或高级里提供查看/编辑。

---

## 五、架构约束（违反即门禁失败，不靠自觉）— 继承老项目，第一天带着

- 后端**路由薄、服务深**：业务全在 `services/`，路由只做 HTTP 适配；`services` 不得 import `routers`（import-linter 契约强制）。
- 前端**组件管渲染、lib 管逻辑**：有状态/编排/纯算法进 `lib/` 并导出（可单测），组件只消费。
- **单一属主**：同一份配置/协议/清洗规则只在一处定义，别处引用。
- 密钥只落被 Git 忽略的 `backend/data/user_state.json`。

### 完成 = 过这六关（缺一不可）
1. `cd backend && ruff check app`
2. `cd backend && lint-imports`（`PYTHONUTF8=1`）
3. `cd backend && python -m mypy @mypy_files.txt`
4. `cd backend && python scripts/check_hardcode.py`
5. `cd backend && python -m pytest -q`
6. `cd frontend && npm run build && npm test`

> 只改后端可跳 6，只改前端可跳 1-5，碰到的那侧必须全过。新功能/改 bug 要补测试。前端改完必须 `npm run build` 重建 dist（后端 :8010 托管静态，不是 :5173 热更）。

---

## 六、Harness 四条 = 本产品的能力，不只是开发规范

本产品本身就是一个替创作者跑 Agent 的 harness，故这四条既约束我们造它、又交付给用户：

- **熵管理**：长篇故事状态会越滚越脏（叙事版技术债）。用**定期摘要 + RAG 剪枝**——正是「超越酒馆数据库、长了就崩」的落点。
- **反馈循环（PreCompletion 检查）**：出一张场景图「完成」前，自动核对是否贴合脊柱（人物一致性校验）——产品级质量闸。
- **Trace 分析**：SSE 已透出 trace。对创作者 = 「AI 为什么这么演/这么画」的可观测与纠偏；对工程师 = 看分派对不对、调提示词。
- **破 Doom Loop**：AI 反复重生成同一场景却不见好时，检测并打断（换方案 / 回问创作者）。加了编程/插件功能后尤其需要。

---

## 七、当前进度

- [x] 从当前项目复制地基到 `d:\tool\Demiurge`（backend 源码、laf_lock、前端骨架；已排除密钥/权重/索引/产物）
- [x] 本文档（目标 + 约束交代）
- [ ] 新建独立 Git 仓库
- [ ] 前端 UI 三模式骨架
- [ ] 核心联动链 ①②③（高潮点识别、结构化提取、一致性组装）
- [ ] 正则 / 预设编辑功能
- [ ] RAG 索引重建策略
