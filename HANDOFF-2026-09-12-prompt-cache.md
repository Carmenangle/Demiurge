# HANDOFF · Prompt 前缀缓存优化（2026-09-12 16:30 · **17:05 更新：P4-roleplay 已实施并验证**）

> 交给新会话接续用。真源：`docs/memory/prompt-cache-design-2026-09-12.md`（设计，§十 10.8 为本轮实施记录）、
> 根 `MEMORY.md`「Prompt 前缀缓存」段、`docs/tech-manual/A/02` + `B/09`（手册）。
> 本文档只管「现在到哪了、下一步做什么」。
>
> **本轮（P4-roleplay）状态**：动态段后置 + 世界书会话内稳定化 **已实施、门禁全绿、真机验证通过**；
> 剩余只有两件小事（输入重复发送、curator 写回丢条目待查）。

## 一、任务背景与已完成

对 Demiurge 的 LLM 请求做前缀缓存优化（三家铁律：static-first / append-only / 工具集冻结 / 禁时间戳）。

| 项 | 状态 | 说明 |
|---|---|---|
| P0 观测（usage 落 trace） | ✅ 已验收 | fabric 会话 `model.usage` 3/3 覆盖；需**真重启后端**（`scripts/start-dev.ps1 -RestartBackend`，`--reload` 不可信） |
| P1 前缀稳定化 | ✅ 已验收 | **fabric run 内实测命中 94.29%**（prompt=21177 / cached=19968）——`messages[0]` 恒静态+进度卡尾置+去 system 双投喂的真机证据 |
| P4 压缩改 append-only | ✅ 已验收 | `fabric_loop.py`：尾部追加替换声明 + 发送前 `_project_messages` 投影；门禁 tests=2576 全绿 |
| P3 摘要 fork | ❌ 裁决不做 | 编译前无父请求；只省「摘要那一次调用」（80% 预算才触发） |
| **通道透传判定** | ✅ **透传** | `tokenrhythm.studio` + `deepseek-v4-flash-0731`。09-04 探针「全 0」是**机制误判**：DeepSeek 命中须完整匹配已持久化的 cache prefix unit，**前两次请求必然不命中**（官方 Example 2），且短请求（49~199 token）切不出单元 |
| **P4-roleplay 动态段后置 + 世界书会话内稳定化** | ✅ **已实施并真机验证**（2026-09-12 16:55-16:57） | `cached>0` **3/5**（基线 0/2，最高 **73.2%**）；字节 LCP **75.1 / 9.0 / 80.5 / 79.8 %**（基线 **7.6%**）；`[0]` 静态头跨 4 轮逐字节一致；分叉点已搬到「`[12]` 上一轮剧情」。详见设计文档 §十 10.8 |
| 输入重复发送（`[30]`+`[32]`） | ⬜ 未做 | 小优化；两者都在动态块之后，**不影响前缀复用**，只是冗余 token |

## 二、roleplay 侧现状（今天 16:11 / 16:16 两轮实测）

### 2.1 结构验证：✅ 符合「只带上一次剧情」

messages 共 33 条，历史区**只有一轮** `[12]user + [13]assistant`（= 上一轮的用户输入 + 回复纯正文）。
更早的剧情不进对话，靠世界书条目检索 + RAG 召回携带。上下文合同实现正确。

### 2.2 缓存命中：真实 cached 0/2（机制性），但字节 LCP 只有 **7.6%**

| 轮 | prompt tokens | cached | 字节长 |
|---|---|---|---|
| 16:11:47 | 21,993 | 0 | 32,654 |
| 16:16:19 | 23,965 | 0 | —（LCP=2477，占上轮 7.6%）|

**前两次 cached=0 是机制**（第 3 轮才见真章），但字节层面只复用 7.6%，第 3 轮也不会高。

> **【16:55 修正】当时判「世界书是最大前缀杀手」是错的。** 后测（`_tmp/verify_struct.txt` / `wb_items.txt`）显示：
> 轮 1/2 的世界书块**都是 23,206 字符且逐字节相同**，它并没有在整块重写。真正的杀手只有一个——
> **`[0]` 那条 7,650 字符的大 system 里塞着每轮变的动态段**（连续性状态 / NPC 目标 / 数据表），
> 它在位置 ~515 处就分叉，把后面的 few-shot、世界书块与上一轮剧情**全部废掉**。
> 世界书块的**真实**问题是「条目集抖动」：一旦 curator 写回或语义命中集合变化，块内顺序就会变。
> 两件事都已修（见第三节），且世界书改的是**顺序稳定性**而不是「挪位」。

### 2.3 附带发现

- **当前输入发了两遍**：`[30]`（预设包裹的 `<user last input>`）与 `[32]`（原文追加）内容重复；修后实测消息序号为 `[30]`/`[33]`，仍在动态块之后，**不影响前缀**；
- supervisor 路由 9ms 完成、无 `model.usage`（本地零 LLM 路由，非缺陷）。

## 三、下一步（按优先级）

1. ~~补第 3 轮~~ ✅ 已并入下面的 5 轮真机验证。
2. ~~roleplay 动态段后置~~ ✅ **已实施 + 世界书会话内稳定化**（用户拍板「实施 + 世界书会话内稳定化」）：
   - 动态块（连续性状态 / 表格召回 / `directive` / 本轮表更新）收集进 `dynamic_parts` → 末尾拼**一条尾部 system**，复用既有 `tail_msgs` 模式；`substitute_macros` 改为**头/尾各作用一次**；尾部顺序 `[动态块][chains_tail][输出纪律][本轮 user]`。
   - `worldbook.assemble_selection_stable`：条目选择规则与旧版**完全一致**（抽公共 `_candidate_entries`），差异只在①排列按「首次激活顺序」单调追加（按**内容 hash** 记序，不按 `source_index`）②裁剪只在尾部**连续** → 保留恒为序列前缀；锚点（keyword/`constant`）永不裁；**不写「省略 N 条」标注**（改走 `Selection.dropped` + trace）。
   - 门禁：后端全量 `--junitxml` **tests=2581 failures=0 errors=0**；定向 180 passed；ruff/mypy/import-linter 全绿。
3. **输入重复发送**（`[30]`+`[32]`）：⬜ **仍未做**。小优化，顺手可做。
4. **新增待查（本次带出，未改）**：**curator 写回丢了 `【全局机制·世界自转与事件登门】` 条目**（自述「本卡最高级驱动机制」）——16:56:26 的 `worldbook.writeback` 之后它在快照书里全文搜 0 命中。是有意裁剪还是回归需单独查；**与前缀改动无关**（前缀改动只读世界书）。
5. **下一根杠杆（需拍板，改预设顺序）**：前缀断点固定在 `chatHistory` 位置 = `prompt_order` **第 34/136** 位，**之后还有 17 个已启用静态片段（6,328 字符声明 / 6,901 实发）+ 我们自加的 `[输出纪律]` 496 字符** → 这批静态文本每轮白付。移到 marker 之前可把可复用前缀从 **79.6% → 94.0%**；余下 6% 是历史轮 + `<user last input>` 包裹 + 动态块，必须每轮变。
   **为什么这不是纯优化**：等于编辑预设 `prompt_order`，会改变「指令相对历史的位置」（预设作者有意分段深度注入），属提示词语义决策，且是用户自己的预设文件。
6. **两个别算进命中率的假象**：① 分母——5 轮里只有 3 轮有资格命中（轮 1 机制、轮 3 curator 写回），**有资格的 3 轮 3/3 命中**；② 上报粒度——三次命中 `cached` 恒为 16,384，而对应字节前缀是 26,453 与 41,045 两种大小 → 该字段不随字节线性，**`50%` 不可读作「只复用一半」**（fabric 那次 19,968 非 2 的幂，说明也不是硬上限）。

## 三·补 真机验证结果（2026-09-12 16:55-16:57，隔离端口 8011）

工具：`_tmp/verify_roleplay_cache.py`（v2）；卡与世界书均 `异世界夺妻马`；预设 `GrayWill-0.46-demiurge`；5 轮。

| 轮 | prompt tokens | cached | 命中率 | 说明 |
|---|---|---|---|---|
| 1 | 22,212 | 0 | 0 | 机制：首个 cache prefix unit 需两次请求后才建立 |
| 2 | 22,375 | **16,384** | **73.2%** | 命中第 1 轮前缀 |
| 3 | 32,420 | 0 | 0 | **curator 16:56:26 写回世界书** → 前缀真变 |
| 4 | 32,730 | **16,384** | **50.1%** | 命中第 3 轮前缀 |
| 5 | 32,812 | **16,384** | **49.9%** | 命中第 4 轮前缀 |

- **相邻轮字节 LCP：75.1% / 9.0% / 80.5% / 79.8%**（改动前基线 **7.6%**）→ **约 10 倍**。
- **分叉点已搬到该搬的地方**：轮 3→4 / 4→5 的 LCP = 41,045 字符，落点 = `[12]` 上一轮剧情那条 user 消息（偏移 7）。即 `[0]` 静态头（1,764 字符，跨 4 轮逐字节一致）+ `[7]` 世界书块（37,793 字符）+ 全部预设指令块**都在可复用前缀内**。
- **唯一那对 9.0% 不是抖动，是内容真变了**：trace `16:56:26 worldbook.update_scope` + `worldbook.writeback`（curator 写回）。逐条比对：`pos1` 从 `【全局机制·世界自转与事件登门】`(1,659 字符) 换掉 → 旧内容 hash 解析不到（该处前缀缩一次）、新内容作为新条目**追加到尾部**（`selected` 14 → 25 条，末位正是它）。**与设计逐字符合**。
- **观测口径注意**：三次命中的 `cached` **恒为 16,384** → 上游按**粗粒度单元**上报，命中量级以 16K token 为台阶，**不要按字节线性外推**。
- **离线复现**：`_tmp/repro_stable_seq.py`（真机那本书 + 同 5 个 query，死 embed 只走 BM25）→ 同一会话连续 5 轮 `selected` 恒为 `[37, 0..12]`，前缀性质 **5/5 ✅**；对照「每轮全新会话」与「无状态 `assemble_selection`」第 1 轮后即丢掉 `37`。

**验证工具踩过的两个坑（下次直接照做）**
1. **代理**：不能直接拿 `proxyUrl` 就用——本机 7897 没在听，v1 因此在 supervisor 首调就 `Connection error`、压根没走到 roleplay。必须与产品同源：走 `app.services.system_proxy.resolve()`，只有 `listening=True` 才用代理，否则直连。
2. **世界书绑定**：世界书不在 `character/<卡>/worldbook.json`（那里只有 `card.json`/`regex.json`），独立书在 `userdata/worlds/<名>.json` → 必须传 `worldbook_name`，否则世界书路径根本没被验到；且 `_repo_worldbook` 快照一旦以空书建立就再也读不到 → **每次重跑先清 `_tmp/verify-repo`**。

## 四、验证工具（都在 `_tmp/`，不入 git）

| 脚本 | 用途 |
|---|---|
| `measure_roleplay_usage.py` | roleplay 逐轮真实 cached + 字节 LCP 并排（本轮基线工具） |
| `check_model_usage_trace.py` | 全 agent 的 `model.usage` 汇总与判读 |
| `measure_prefix_reuse.py` | 全量 trace 相邻轮 LCP（含 supervisor/answer） |
| `dump_system_share.py` | dump 各 agent 相邻两轮 system 全文与分叉点 |
| `measure_cache_hit.py` | ⚠ 09-04 旧探针，**结论已作废**（短请求测不出 DeepSeek 机制），仅留档 |
| `smoke_recipe_creation.py` | 固化流程自创建冒烟（今日已适配首轮固化询问，10/10 PASS） |
| `verify_roleplay_cache.py` | **真机 5 轮**（隔离 8011 + 独立 output_dir + `thread=p4-cache-verify`）：真实 cached + 相邻轮字节 LCP + 首条 system 一致性 + 尾部 system + 世界书前缀性质（v2 已修代理与世界书绑定两个坑） |
| `explain_verify_lcp.py` | 定位每对请求的**分叉落在第几条消息、哪个偏移**（只读 trace） |
| `repro_stable_seq.py` | 离线复现稳定序列的 append-only 性质（真机那本书 + 同 5 个 query；对照组：每轮新会话 / 无状态 `assemble_selection`） |

## 五、红线（接手前必读）

- 任何动态内容**不得写回 `messages[0]` 或静态 system 前缀**；
- 压缩**不得就地改写历史**（P4：追加声明 + 投影，三发送点都要投影）；
- **世界书注入不得写「省略 N 条」这类随轮变化的标注**（会破坏「保留部分恒为前缀」；省略数只走 `Selection.dropped` + trace）；
- 判「通道不透传」前先排除 DeepSeek 机制（前两次不命中 + 短请求无单元）；**`cached` 是粗粒度单元上报（本样本恒 16,384），不要按字节线性外推**；
- 判绿只认 `--junitxml`；后端改代码后必须 `start-dev.ps1 -RestartBackend` 真重启；
- 溢出恢复链（400 判死已存在，`roleplay_turn.py:455/521`）的「压缩后重试」**尚未立项**——无出血证据（trace 搜上下文超限 0 命中），排期靠后。
