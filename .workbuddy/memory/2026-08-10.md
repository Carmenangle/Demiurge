# 2026-08-10

## 自动插画 LoRA、多人角色与写实误生成

- 修复对话未触发生图、资产进入小仓库后模式回退、仓库改名后新建同名仓库继承旧对话；回归测试覆盖真实失败路径。
- `single`：在场角色命中角色 LoRA 就只使用角色 LoRA；无命中才回退 Ogipote。`multi`：固定加载 Ogipote 默认风格并叠加全部在场角色 LoRA。冷倾雪＋虞妙玥对应 Ogipote＋柳世熙＋蔡秀晶。
- 白给谷最新真人图的 Comfy 历史确认：柳世熙 LoRA 已正确加载，问题不是选错 LoRA；其元数据无触发词且数据库标为 `not_required`，作品运行态仍为中文 `krea2`。Krea2 Profile 故意不锁真人/二次元，角色 LoRA 单独作用会落入写实域。
- 需要二次元且采用英文 tags + 英文描述的作品应通过「多元数据插入」保存 `anima_tags`。不要直接改 `backend/data/user_state.json`，运行中的前端会以旧内存设置覆盖。白给谷 SAVE01 已从界面保存并回读为 `anima_tags`。
- 后续真实 Trace 出现 `local_scene_fallback, actor_count=0`：正文只用代词且计划缺失时未读取状态 `[在场] 冷倾雪`，导致单 LoRA 错误回退 Ogipote。现已把 `[在场]` 纳入角色精确恢复；状态仍无角色才允许风格兜底。
- 完整生图 Profile 已移出主 Roleplay：主生成只产高潮短画面计划；合规 Anima 计划在后端本地编译，正常路径 0 额外 LLM，失效时才携带当前 ST/防拦截预设独立生成且不带历史。有效英文提示词后拼接的中英文拒答只裁尾缀；整段拒答才降级。
- 计划缺失时的画幅不再固定：按多人物关系、脸部特写、横向动作、纵向/全身动作等纯规则从固定比例集合选择，再由前端按用户最长边换算实际宽高。
- 对话无法生图的真实根因：主提示允许“无适合高潮则省略计划”，而后端普通 `dialogue/action` 无计划就记 `scene_not_triggered`。ComfyUI 自动插画现要求每个真实剧情回复选择本轮最强视觉高潮；主模型仍漏计划时，以 `missing_plan_fallback` 从可见正文选段并发请求，隐藏思考仍不参与。
- 第三次真实故障中，主计划把末尾山道/烽火台外部钩子误当高潮并把主体写成“我”，最终提交仅加载 Ogipote，提示词退化为 `adult woman ... walking`。现在低强度结尾计划会重定向到正文高分高潮段，错误主体、镜头、构图和提示词一并废弃，角色从纠正段与 `[在场]` 重建；Trace 记录 `actors/plan_retargeted`。Anima 使用风格 LoRA 时内容行机械加入手绘二次元媒介锁定词。
- 同日晚间 `local_scene_fallback` 再现预提交永久 pending：Trace 只有 `illustration.request`，Comfy 队列为空且无 `illustration.submitted/failed`。根因是无主计划的 Anima 没有本地写 `profile_prompt`，前端独立 Profile 在页面导航/刷新后失去计时器。现改为所有 Anima 都后端确定性编译；刷新恢复会删除无 `promptId` 的孤儿槽并记录 `resume_unsubmitted`，当前卡死槽已清除。
- LoRA 第四次真实根因：最新 `main_plan_retargeted` 请求仍为 `actors=[]`，最终只提交 Ogipote。真实状态仅在 `<status>` 快照含 `[在场] 冷倾雪`，`character_state.叙事` 没有 `在场`；旧代码只读 `_narr`，旧测试还 mock 了该结果，形成假绿。现改为只解析快照 `[在场]` 单行并与已配置角色名交叉匹配；失败测试由红转绿，模板最终 API 图节点 19 为 `krea2_柳世熙.safetensors@1.0`，无 Ogipote；Trace 新增 `actor_candidates/status_actors`。

## Krea2 单一六维英文转译提示词

- 旧合同把 SFW 固定为中文、NSFW 固定为六段中文加两句英文，并强制 300–600 字十段顺序和非常规机位；这与 Krea2 的长自然语言能力、用户提供的专用领域模板和英文输出目标冲突。
- 最终合同进一步收敛为一份剧情高潮转译模板：不做场景分类，不含角色设定图/超巨型生物规则，也不按 SFW/NSFW 切换。按构图留白、角色服装、摄影镜头透视、有机材质质感、光影层次色彩、画质质量完成度六维顺序输出单段纯英文。
- “画质质量与完成度”只描述解剖稳定、材质精度、干净边缘、高图像保真度和整体完成度，不代表真人写实；普通剧情继续剥离真人/动漫/3D 媒介锁定词，实际画风由 LoRA 决定。中文、拒答、错误段落等硬失败使用纯英文剧情兜底。
