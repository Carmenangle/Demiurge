# 2026-08-07

## 了解项目（上午）
- 通读 README/AGENTS.md/ARCHITECTURE.md/docs/memory，整理项目全貌并建立 .workbuddy/memory/MEMORY.md。

## 续完「插画视觉命题 + Trace 可观测性」修复（晚间）
- 中断前状态：实现与双端测试已全部写入工作区（锚点状态变化加权、resolve_illustration_anchor 纠正+废弃错误内联 Profile、turn_id 贯穿、illustration-submission 端点、前端 reportIllustrationSubmission）。
- 本次完成：
  - 核对全链路 wiring（roleplay_turn → chat_stream_protocol → 前端解码 → submitIllustration → 上报端点），确认上报失败双保险（前端 .catch + run_trace.emit 内部吞异常）。
  - 全量验证：后端 pytest 1121 全绿、前端 vitest 312 全绿、npm run build 通过、ruff/mypy(26 文件)/import-linter(13 合同)/check_hardcode/git diff --check 全过。
  - 文档同步：ARCHITECTURE.md 补 scene_illustration/image_prompt_extract/chat_stream_protocol 三行合同与异步闭环上报段；新增 docs/memory/illustration-anchor-and-submission-trace.md 并登记 MEMORY.md 索引。
- 经验：backend 测试须 `cd backend && .venv/Scripts/python -m pytest tests`（从仓库根跑会 No module named 'app'）。

## /w 工作流大图残留修复（2026-08-08 早晨）
- 在 ComfyUI 打开大工作流后 /w 选模板，NodeCard 所有节点 IFrame 被 ComfyUI localStorage 会话恢复抢占画布，始终显示大图而非单个节点。
- 第一轮修法：`clearComfyStorage()` 清理 localStorage/sessionStorage 的 workflow key。LOCK 模式双点调用。
- **第二轮补修**（用户反馈仍卡在 Krea2 工作流）：增加 IndexedDB 清理（`_clearComfyIndexedDB` 遍历删除含 workflow/comfy/localforage/keyval 的数据库），FULL 模式也补上 setup 和 load 双点调用。
- 修改文件：`comfyui-ext/laf_lock/js/laf_lock.js`；前端 312 全绿；ARCHITECTURE + docs/memory 同步。
