# 2026-08-11

## Krea2 通用兜底导致角色 LoRA 失真

- 真实生成 `workflow_eeb034d37d1dc42c3b38bac9.png` 的元数据与 ComfyUI 日志确认：`krea2_柳世熙.safetensors@1.0` 在两阶段均挂载 256 patches；故障不是 LoRA 选择，而是最终正向提示词逐字退化为 `_krea_scene_fallback` 通用段落。
- `main_plan_retargeted` 原先会把 Krea2 `request_prompt` 清空；独立 Profile 连续拒答后，兜底没有冷倾雪外貌、高潮动作、插画媒介或无噪点质量约束，Krea2 因而回到真人通用脸并产生明显颗粒。
- 修复合同：主计划除 anchor/subjects.name 外使用简洁英文；解析层保留归一化 subjects；高潮重定向重建英文动作底座且保留稳定 subjects；角色 LoRA 兜底机械锁定既有五官、眼形、发型轮廓、体型、手绘二维插画媒介、干净边缘与无噪点高保真完成度。
- 回归覆盖真实接缝：静态错误锚点被重定向后 `scene_spec.draft_prompt` 不为空且 subjects 保留；连续拒答的 Krea2 角色 LoRA 兜底不得含 photorealistic/anime 等锁词，但必须含 identity lock、hand-drawn 2D 与 noise-free quality。完整后端 1165 项通过。
- 补记原始提示词合同：剧情高潮与角色条目外貌由 Profile 融合；实际 LoRA 的触发词和作者质量建议必须在 Profile 完成后查表、筛选、机械注入并去重，禁止交给模型改写。冷倾雪当前绑定的 `krea2_柳世熙.safetensors` 库记录为手动确认无触发词，且作者建议为空，因此运行时不应伪造第三来源。
- 最终回归补齐：四 Profile 的正常输出和确定性兜底都必须纯英文；Krea2/Anima/自然语言/Niji 均从混合语言字段保留具体英文高潮与外貌事实。真实模型四 Profile 均 direct 且保留动作、长黑发、齐刘海和细长红眼。
- 真实 Krea2 模板单角色任务约 80 秒完成；Comfy 历史确认 `krea2_柳世熙.safetensors@1.0` 同时接入两个采样器，704×1024，提示词无 Ogipote，产物为手绘二次元。多 LoRA 干运行确认 Ogipote→柳世熙→蔡秀晶链尾统一接采样器。
- 七项联动真实 HTTP 验收通过事实/认知/CI/偏好/全状态快照/分支实验/能力撤销及 Chroma 克隆；最终门禁后端 1202、前端 335、Playwright 3，静态合同和生产构建全绿。

## 正文与完整插画 Profile 同轮生成

- 真实拒答 Trace 的根因是完整 Profile 被拆成正文后的第二次模型调用，丢失对话上下文；`I won't generate` 又不在拒答正则内，拒答曾被误当 Krea2 成稿。
- 修复为主 Roleplay 一次输出：`<content>` 独立满足预设/用户篇幅，隐藏 `<illustration>.profile_prompt` 同轮生成当前 Profile 完整英文成稿。隐藏块剥离后只进入 `scene_spec`，不显示、不计正文长度。
- 显式 Roleplay `maxTokens` 在正文额度外为 Krea/Natural 追加 1000、Anima/Niji 追加 800；未显式设置时沿用模型默认上限。
- `<illustration>` JSON 在解析前复用正文 `AI_OUTPUT` 正则，成稿再走 `IMAGE_PROMPT` 清洗和 Profile 门禁，避免防拦截预设改变结构后无法解析。
- 四 Profile 的坏 JSON、漏块、拒答或格式失败均从同轮事实确定性编译，不再依赖前端第二次文本调用；锚点被纠正时废弃描述错误桥段的同源成稿。
- 拒答正则新增 generate/create/produce/write/transform/provide/fulfill，并确保 `I'm Claude Code. I won't generate...` 整行丢弃。
- 真实模型无 Comfy 验收：229.58 秒返回，`<content>` 可见正文 623 字、隐藏 Krea 成稿 1182 字，计划存在、无泄漏、无拒答。首版因视觉事实门禁未通过会退通用兜底；随后增加“仅漏具体事实时机械补齐并保留原成稿”的窄修复，硬错误仍拒绝。
- 最终门禁：后端 1251、前端 343、Ruff、17 条 import-linter、mypy 39 文件、硬编码检查及生产构建通过。
