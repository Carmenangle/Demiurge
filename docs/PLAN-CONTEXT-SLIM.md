# PLAN-CONTEXT-SLIM — 剧情模式上下文瘦身合同（2026-08-30 用户定稿）

## 背景（trace 实测）
真实正文轮输入 37k 字符：预设 10.6k（动不了，用户明确）+ wire:7 单块 15k（history_after 注入，需定位）+
插画/视频/音频尾部合同约 7k。症状：think 泄入 status、正文截断、模型降智。

## 用户定稿的上下文合同（剧情模式）
1. 历史：只进上一次剧情轮；且过滤到只有正文——剥离 think/status/encounter/插画视频音频 JSON 块/骰点块。其余更早轮次一律不进上下文。
2. 记忆召回：召回源=表格的纪要表中的简要内容；按与本次对话相关性排序取 Top-10 条。
3. 世界书：选择性开启的全局条目照常注入；系统判定机制条目全程恒定开启；其余条目只注入与本次事件相关的（相关性判定，非全量）。
4. 预设：不动（wire:0 预设主系统提示保持原样）。

## 已完成的前置修复（本轮已生效）
- 正文轮 max_tokens 未配置时跟随「历史上下文上限 tokens」设置（>0 精确采用；0=不传；缺失兜底 32768）
- 思考纪律软引导已注入 tail_msgs（禁回退重确认/动笔后不回退/不复述设定历史）
- 非高潮路径 firstlast 请求恒定携带 scene_spec（前端帧编译不再被跳过）
- 帧编译失败禁止中文原文直提（显式失败槽）

## 实施锚点（代码位置）
- 历史组装：agent_graph.stream_multi_agent → agent_context.recent_history(...)（agent_graph.py ~3050）
- 召回：agent_graph 记忆召回块（~2212 起）；wire:7 15k 块 = history_after 注入链（~600-840 组装）
- 世界书：世界书检索链；系统判定条目恒开逻辑加在条目筛选处
- 同轮成稿剥离：same_turn 内联 profile 合同（agent_graph ~1570-1615）改走 profile_llm_fallback 独立链（已实证 status=ok）

## 验收
- 输入字符对比：37k → 目标 <20k（预设 10.6k + 上轮正文 ~3k + 召回 Top-10 ~2k + 世界书相关 ~2k + 尾合同 ~6k）
- 正文完整率 100%（无截断）；think 不再泄入 status；首尾帧回填+资产库入库正常