# HANDOFF — Demiurge 自动插画链路修复 + 剧情模式上下文瘦身（2026-08-30 凌晨）

## 给新会话的第一句话
工作区 D:\tool\Demiurge（后端 FastAPI 8010 分离式运行中，前端 vite 5173）。先读 docs/PLAN-CONTEXT-SLIM.md（用户定稿的上下文合同+实施锚点），再按本单继续。

## 必读门禁（AGENTS.md 已在工作区）
- 后端改动：py_compile + ruff（.venv\Scripts\ruff.exe）+ pytest 相关子集 + 分离式重启后端验证（Stop-Process 全 uvicorn python 进程 → Start-Process .venv python -m uvicorn app.main:app --port 8010 --reload --reload-dir app，WorkingDirectory backend）
- 后端 uvicorn --reload 曾静默失效过两次：改完代码必须重启后端再让用户测试，不能只报「已修复」
- trace 诊断：backend/data/logs/agent-trace.jsonl（JSONL，python -X utf8 解析，别用 PowerShell ConvertFrom-Json 查大文件）
- 插桩已就位：agent_graph.py illustration.pipeline catch 会发 error_type/error_trace

## 用户定稿的上下文瘦身合同（待实施，全部待办）
1. 剧情模式历史：只进上一次剧情轮，且过滤到只剩正文（剥 think/status/encounter/插画视频音频 JSON/骰点块）；其余轮次不进上下文
2. 记忆召回：只召回纪要表简要内容，按相关性取 Top-10
3. 世界书：全局条目照常 + 系统判定条目恒开 + 其余条目按本次事件相关性选择注入
4. 预设一字不动（用户明确）
5. 剥离正文轮「同轮成稿」内联插画 JSON 义务（same_turn profile/plan），全走 profile_llm_fallback 独立链（已实证 status=ok）
验收：输入 37k→<20k 字符；正文零截断；think 不泄入 status；首尾帧回填+资产库入库正常

## 今天已落地的修复（都已验证+后端已重启生效）
1. max_tokens：正文轮未显式配置时跟随「历史上下文上限 tokens」设置（>0 精确用；0=不传；缺省兜底 32768）——agent_graph.py _roleplay_sampling
2. 思考纪律软引导注入 tail_msgs（禁回退重确认/动笔后不回退/不复述设定历史）
3. 非高潮路径 firstlast 请求恒带 scene_spec（agent_graph.py illustrate_req 组装块 setdefault）——修「帧编译被跳过」
4. useChatSession.ts 帧编译失败禁止中文原文直提 ComfyUI（显式失败槽，可重新生成）——修 8 字符对白当正向提示词的垃圾图
5. agent_graph.py illustration.pipeline 插桩 error_type/error_trace
6. 首帧锚点=正文第一段末（scene_illustration.first_frame_anchor_offset + agent_graph 特判 firstlast）；think 剥离；提交 60s 超时+重试；ComfyUI 救回

## 今天踩过的坑（防复发）
- 19:03 pytest 的 monkeypatch 泄漏进 uvicorn spawn 子进程（堆栈实锤指向 tests/test_agent_dispatch.py:2313），写回必抛「writeback failed」→ 生图请求不下发。教训：测试与活服务共享进程树会互相污染
- DSH 崩溃（退出码 130）会带走 DSH 托管的后台任务；已装崩溃取证层：node_modules/@deepseek-ai/dsh/lib/crash-guard.js（bin.js 头部 import），崩溃后看 AppData/Roaming/Deepseek Harness EAC/logs/dsh-web-crash.log
- 并行 AI 会话曾对活环境（8010/8188）跑模拟测试互踩（用户已叫停）；ComfyUI history 里有它的 e2e 任务（QRQ 开头的提示词）
- krea2_柳世熙 LoRA 无需触发词（用户确认），工作流注入正常（model=1.0）
- 用户设置语义：「历史上下文上限 tokens」0=无限（不传 max_tokens）、>0=精确采用

## 当前已知未解决问题（按优先级）
1. wire:7 15k 单块 system 注入（history_after）来源未定位——下轮第一步
2. 上下文瘦身合同全项待实施
3. 提交链挂死仍需手动刷新才走恢复路径（看门狗未做）
4. 双帧串行渲染 5-8 分钟（可改并行提交砍半）
5. 帧编译 LLM 校验口径偏严（可视事实未落实/词数 80-500），兜底产物合法但偏平

## 关键文件
docs/PLAN-CONTEXT-SLIM.md（合同+锚点）· agent_graph.py（管线/采样/插桩）· image_prompt_profiles.py（帧编译/兜底）·
useChatSession.ts（submitIllustration/帧循环 ~1071-1260）· api/comfyui.ts（60s 超时）· story_frames.py（双锚点提取）