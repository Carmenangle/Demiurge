# Demiurge Domain Context

Demiurge 是本地优先的交互叙事与 ComfyUI 创作环境。本页只定义稳定领域语言，架构实现见 `ARCHITECTURE.md`，故障与验收历史见 `docs/memory/`。

## Language

**大仓库（Work Repository）**:
一部作品的顶层容器，持有共享绑定与多个相互隔离的小仓库。
_Avoid_: 项目、父会话

**小仓库（Story Line）**:
一条可独立对话、记忆、状态、资产与分支的正式或实验剧情线。
_Avoid_: 会话文件夹、存档槽

**回合快照（Turn Snapshot）**:
某个已完成剧情回合的不可变全状态副本，包含作品文件、SQLite 状态与作品专属向量集合。
_Avoid_: 消息 checkpoint、当前状态副本

**反事实实验（Scenario Experiment）**:
从同一回合快照派生的一至三条隔离小仓库，用于比较不同剧情选择且不自动合并。
_Avoid_: 消息重生成、临时草稿

**剧情高潮（Illustration Climax）**:
当前回复中最强的单一视觉命题，是自动插画动作、主体、构图与画幅的事实来源。
_Avoid_: 消息尾句、静态人物肖像

**提示词 Profile（Prompt Profile）**:
把同一剧情高潮与角色稳定外貌转换为某一图像模型格式的规则集合。
_Avoid_: LoRA 提示词、剧情续写模板

**LoRA 元数据（LoRA Metadata）**:
按实际加载文件名保存的触发词、建议权重与作者质量建议，在 Profile 完成后机械注入。
_Avoid_: 角色外貌来源、模型生成的触发词

**世界事实（World Fact）**:
具有有效回合区间、证据和显式替代关系的客观实体事实。
_Avoid_: 角色实时状态、纪要推测

**角色认知（Character Belief）**:
指定角色在指定回合对某项事实的知道、相信、怀疑、误解、隐瞒或未知状态。
_Avoid_: 世界事实、角色 core

**Narrative CI**:
对候选正文与权威状态进行非阻断一致性诊断并保存证据和处置状态的剧情检查。
_Avoid_: 自动改写器、内容分类器

**Procedure Skill**:
从已脱敏 Trace 提炼、经用户审核并凭短期能力租约执行的机械流程。
_Avoid_: Prompt Skill、任意 Agent 工具权限

## Relationships

- 一个**大仓库**包含一个或多个**小仓库**。
- 一个**小仓库**在每个完成剧情回合至多生成一个去重的**回合快照**。
- 一个**反事实实验**只从一个**回合快照**派生，并创建一至三个隔离**小仓库**。
- 一个自动插画先由**剧情高潮**与角色稳定外貌生成**提示词 Profile**，再合并实际加载的**LoRA 元数据**。
- **世界事实**描述客观发生了什么；**角色认知**描述每个角色认为发生了什么；**Narrative CI**只诊断两者与候选正文的冲突。
- 一个**Procedure Skill**必须先审核，再由短期能力租约授权每一步执行。

## Example dialogue

> **开发者：**“用户从第 42 回合创建分支时，复制当前消息就够了吗？”
> **领域专家：**“不够。必须从第 42 回合的回合快照创建新的小仓库，否则消息属于过去、世界状态却来自现在。”

## Flagged ambiguities

- “仓库”曾同时指大仓库和小仓库；界面可继续统称，架构讨论必须明确使用**大仓库**或**小仓库**。
- “快照”曾同时指消息 checkpoint 和全状态副本；只有包含文件、SQLite 与向量集合的不可变副本称**回合快照**。
- “角色记忆”曾混指客观事实、角色状态和主观认知；分别使用**世界事实**、角色实时状态与**角色认知**。
